# Day 31 : Gate 9 Writeup

## Week 9 summary
We built a decoder using python where if we give it a hex number, it will tell what the instruction is from what we have covered from R, I and S-type instructions. We did 12 hand encodings like we took 12 different instructions then wrote all of them in binary first in the correct way for its type and then also computed its hex values.

## Vending machine revision
Rebuilt it again from scratch. The syntax for enum felt a bit confusing and initially confused a little for the states like I added a state even though the state was not required which I solved during the building part.

## Key concepts
Given a hex instruction we learned how to extract the correct bits and take information from that including the opcode, different registers and func3 func7 or the immediate values.<br>
We learned specially about 3 different instruction types like R, I and S-type instructions R-type means register to register data transfer, I-type means having immediate value in the instruction itself and S-type is storing to memory<br>
We learned that for different instructions the bit positions for the registers were made at same position even if it needed to split the immediate values so that we dont need extra logic to decode the register address