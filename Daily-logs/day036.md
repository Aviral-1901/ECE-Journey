# Day 36 : Light Review

## Recalls
- If all the signals are not handled for combinational circuits then a latch is infered so every signals should be handled well and include default case so that everything is handled well.

- Registers reads are combinational because it allows to read the value, complete the calculation and signals to be stable before the clock edge for writing and if it were sequential then on one cycle we would be reading and doing calculation then have to wait for next cycle so basically we would be loosing a cycle.

## Confusion
I wrongly labelled R-type instruction as I-type instruction even though I knew the concept behind those and it was a labelling error.